new PoolView(opts){
    opts.collection;
    registerForAnimationFrame
}

update(){
    // find end of the reuseViews hits the provisionLimit
    // if lastReuseViewBottom above the provisioningThreshold return;
    
    // move container to fragment
    
    // loop through all the reuseView
    for (item in reuseViews)
    {
        // get the bottom a reuseView item
        
        // if pass the disposableLimit then remove
    }
    
    // while renderedLimit above the provisioningThreshold
    
    while (renderedLimitNotHit)
    {
        // get the cursor from the poolview
        
        // tell the view to render the next item, set top position
        poolview.render function(idx, top)
        {
            // get backbone model
            
            // get item from dequeue
            
            // call repaint
            
            resueView.repaint function(top, model)
            {
                // set position
                
                // set all imgsrc to null
                
                // call the update functions using cache selector
            }
            
            // append child to container
        }
        
        
        
        // if newRenderLimit equals oldRenderLimit throw error
        
        // set new renderLimit
    }
    
    // put container back on DOM
}


